#!/bin/bash   - это строка вызывает интерпретатор bash
#SBATCH -p MOF   - это строка говорит, чтобы задача запускалась на разделе с именем MOF
#SBATCH -n 72   - это строка говорит о количестве процессов (ядер), которое нам нужно
#SBATCH -J st107   - это название задачи, которое мы придумываем сами

srun vasp541-std.04 > out  - это вызов провграммы VASP и файла out для печати выходных данных